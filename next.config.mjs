/** @type {import('next').NextConfig} */
const nextConfig = {
  // No extra config needed — Next.js 14 works out of the box.
  // This file exists so you have a place to add things later (image domains, etc.)
};

export default nextConfig;
